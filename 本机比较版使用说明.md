# TCIM 本机比较版使用说明

## 1. 这个版本是什么

这是与第一版完全分开的本机研究比较版，架构为：

> Dialogue Agent 主导＋新五表＋Evidence State

以下部分直接沿用第一版：

- 教师资料；
- 10 道情境排序测评及 20 分钟倒计时；
- 计分；
- 根据结果与作答过程确定 3 个访谈情境；
- 每个情境 10 分钟访谈时限；
- 情境图片、题干、选项和教师排序展示；
- 访谈记录、草稿与最终回执。

核心变化只发生在 AI 访谈：旧五表、旧本地问句模板和旧固定路线不再进入运行链。每一条专业问题都必须来自 Dialogue Agent；生成失败时暂停并允许重试，不用本地专业模板冒充 AI。

## 2. 一键启动

双击根目录中的：

`启动本机比较版.cmd`

程序会启动两个仅监听本机地址的服务，并打开：

`http://127.0.0.1:5173`

不用时双击：

`停止本机比较版.cmd`

第一次运行若尚无网页依赖，启动程序会自动准备；以后直接打开即可。

## 3. 模型选择

模型配置文件为：

`local-dialogue-server/.env`

该文件已被 Git 忽略，只保存在本机。密钥不要写入网页、源代码、截图、聊天记录或 GitHub。

最方便的做法是双击根目录中的 `配置大模型.cmd`，再按提示选择 Kimi K3、OpenAI、自动选择或仅工程演示。密钥输入时不会显示。配置完成后，先双击 `停止本机比较版.cmd`，再双击 `启动本机比较版.cmd`。

### 3.1 不填写密钥

程序进入 `mock` 工程演示模式。页面必须由使用者明确确认后才会生成演示问句；演示会话单独保存并标记 `simulationOnly`，不计入正式比较统计和教师报告，也不代表真实 AI 访谈质量。

### 3.2 沿用第一版 Kimi K3

把 Kimi 国内开放平台密钥填在：

`MOONSHOT_API_KEY=您的密钥`

其余默认值已经设置为：

- 地址：`https://api.moonshot.cn/v1`
- 模型：`kimi-k3`
- 推理强度：`high`

第一版腾讯云中的密钥只存在云函数环境变量，GitHub 不包含它，因此本机版无法自动读取。需要在本机配置一次。

### 3.3 使用 OpenAI

填写：

`OPENAI_API_KEY=您的密钥`

默认模型为 `gpt-5.6-sol`。如果 Kimi 和 OpenAI 两个密钥同时存在，自动模式优先 Kimi；如要强制使用 OpenAI，再填写：

`TCIM_DIALOGUE_PROVIDER=openai`

修改 `.env` 后，先停止再重新启动比较版。

## 4. 新运行关系

一次教师回答后的运行次序是：

1. 教师原话先保存到本机。
2. Orchestrator 把本题情境、新五表紧凑卡、历史对话、剩余时间和 Evidence 摘要交给 Dialogue Agent。
3. Dialogue Agent 自主决定承接、澄清、深化、转向或结束，自主生成一条教师可见回应。
4. Dialogue Agent 可以追随五表之外的新线索，并建立表外开放主题；五表中的 AFFORDANCE/MONITOR 只是建议，不强制路线。
5. Dialogue Agent 同时可以提出 Evidence 候选，但候选必须包含合法的 `evidenceClaimId＋understandingId` 和本轮教师原话的逐字片段。
6. 模型外 Evidence Validator 再校验；它会检查 ID 映射、教师原话片段、RO0—RO4 来源、允许来源与政策版本，并确定证据等级上限。模型只能提议等级，单轮回答不能直接成为 `HIGH_QUALITY`；无效候选只进入拒绝日志。
7. 只有安全、隐私、教师明确退出、答案泄露和单轮多问等硬边界可以阻断模型结果。

因此，“理解教师和决定下一步”主要属于 Dialogue Agent；“什么能够成为可追溯证据”不由模型单方面决定。

## 5. 新五表放在哪里

五个原始 Excel 的只读副本位于：

`config/new-five-tables/source/`

网页运行时读取的是预编译文件：

`web/src/generated/tcim-new-five-tables.runtime.v0.1.json`

其配置指纹为：

`f729a09c81ab1937aa09544b61d13bad2673de04af067c3ad892f48f036c8ab7`

运行时不会逐轮读取 Excel，也不会加载旧五表。这样既避免版本混用，也减少访谈延时。

## 6. 数据保存在哪里

- 浏览器侧会话和逐轮 Evidence State：浏览器本机存储。
- 本机服务侧教师、测评、遴选、草稿与访谈回执：`local-dialogue-server/.runtime-data/state.json`。
- 运行日志：`.local-runtime/`。

以上目录均不提交 Git。当前是研究比较版本，不应存放未经研究授权的真实教师数据；正式试用前还要增加研究知情同意、删除、导出、加密和保留期限机制。

## 7. 给程序员的 HTTP 接口

本机 Dialogue Agent 地址：`http://127.0.0.1:8787`

- `GET /health`：模型与服务状态；
- `POST /v1/dialogue/first`：首问；
- `POST /v1/dialogue/next`：教师回答后的下一轮；
- `POST /v1/dialogue/turn`：统一的 first/next 接口；
- `POST /call`：本机版网页网关，支持资料、测评、确定性选题、草稿和访谈上报。

服务只接受 localhost/127.0.0.1 来源，API 密钥只由服务端 `.env` 读取。OpenAI 请求设置 `store:false`；响应记录 provider、model、prompt version、usage 与 latency，便于后续比较。

## 8. 已完成的自动检验

- 本地 Dialogue Agent 服务：18 项测试通过；
- Dialogue Agent/Evidence State 领域层：20 项测试通过；
- 教师画像报告：只读取活动的 canonical Evidence；演示记录和旧 mock 记录均被排除；
- 新五表运行时：10 类负向夹具全部被拒绝；
- 原测评计分：240 个“题目×排列”与第一版一致；
- 比较版网页：构建通过；
- 实际浏览器流程：资料保存、10 题作答、提交计分、3 情境遴选、首问、教师回答、下一问、情境切换与明确演示入口均已跑通。

## 9. 当前需要保留的研究警示

当前新五表属于 AI 模拟复核、研究试验配置。确定性验证还给出三类非阻断警告：

- 59 个父能力标签尚未归一为正式 ID；
- 55 条证据政策没有显式 `pathRefs`；
- 21 条对话政策的 `type/runtimeUse` 不一致。

本比较版避免依赖这些缺口作硬路由：父标签和路径只作模型视野，AFFORDANCE/MONITOR 只作软建议，Evidence 写入仍按合法命题 ID 与原话片段校验。它可以用于架构比较和潜力试验，但不能据此宣称教师能力结论已经具有正式测量效度。
