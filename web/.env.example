# 面向网页端的 HTTPS 网关（例如 https://api.example.com/gsyg-web）。
# 当前可部署 gsyg_webGateway 作为「匿名演示」网关：按浏览器 Cookie 隔离，
# 不验证手机号、不支持跨设备恢复，禁止用于正式数据采集。
# 不要让网页直接调用依赖 wxContext.OPENID 的小程序云函数。
VITE_WEB_API_BASE_URL=
