# 数据导入格式规范

> 目标:支撑三步全自动 —— ①老师作答后**自动评分** ②**自动筛选** 3 道访谈题 ③**自动驱动 AI 问答**。
> 规范格式为 **JSON**(程序导入用);研究团队用 Excel 模板填写,再由导入脚本转 JSON + 校验 + 落库。所有文件带同一 `version`,教师作答绑定当时版本以保证可复现。

---

## 0. 文件清单与用途

| 文件 | 喂给哪一步 | 说明 |
|---|---|---|
| `1_questions.json` | 展示 + 评分 | 题干、4选项 |
| `2_score_table.json` | **自动评分** | 每题每种排列的 0-4 分(查表) |
| `3_indicator_map.json` | **自动筛选** + 报告 | 主/次二级三级指标、观测点 |
| `4_selection_rules.json` | **自动筛选** | R/P/G 阈值与覆盖规则 |
| `5_knowledge_base.json` | **AI 问答** | 每题 13 表结构化:E/P/T/Q/锚点/建议 |

数据流:
```
作答排序 ──2──▶ 自动评分(总分/RD) ┐
过程日志 ─────▶ P-IVI/过程标签    ├─3+4─▶ 自动筛3题 ──5──▶ AI问答 ─▶ 报告
```

---

## 1. `1_questions.json` — 题库

```json
{
  "version": "2026-07-02-v1",
  "items": [
    {
      "item_id": "Q1",
      "kb_id": "GSYG_01",
      "title": "篮球架玩水",
      "age_group": "未明",
      "game_type": "户外自主游戏/玩水",
      "conflict": "自主生成游戏与规则、安全、场地适宜性之间的张力",
      "stem": "操场上新安装了一些篮球架，幼儿经常在这里投篮……请按最理想到最不理想排序。",
      "options": {
        "A": "看到幼儿玩得很开心，不干预他们的玩水行为，待兴趣减弱后再讨论玩水的适宜性。",
        "B": "表扬幼儿的新发现，并询问在篮球架旁玩水是否合适，提议幼儿可以换个地方玩水。",
        "C": "参与到幼儿的“粉刷”游戏中，逐步引导幼儿到适宜玩水的地方继续开展游戏。",
        "D": "提醒幼儿篮球架是用于开展运动的，与其他幼儿一起召唤他们打篮球，转移他们的关注点。"
      }
    }
  ]
}
```
> `options` 的 key 固定为 `A/B/C/D`,是排序与评分的锚点,导入后不可改。

---

## 2. `2_score_table.json` — 赋分(自动评分核心)⭐

**关键决定:用「完整排列字符串 → 分数」的映射**,而不是"第几行"。这样彻底避开"24行对应哪种排法"的口径歧义。每题恰好 24 条(4!=24)。

```json
{
  "version": "2026-07-02-v1",
  "scale": [0, 4],
  "scores": [
    {
      "item_id": "Q1",
      "by_order": {
        "C>A>B>D": 4,
        "A>C>B>D": 4,
        "C>A>D>B": 3,
        "A>B>C>D": 2,
        "B>C>A>D": 2,
        "D>A>B>C": 1,
        "D>C>B>A": 0
      }
    }
  ]
}
```
- `by_order` 的 key = 教师从**最理想到最不理想**的选项序,用 `>` 连接;value = 0-4 分。
- **每题必须齐全 24 种排列**,导入脚本校验不重不漏。

### 自动评分伪代码
```js
function scoreItem(itemId, ranking /* e.g. ["C","A","B","D"] */, table) {
  const key = ranking.join(">");
  return table[itemId].by_order[key];   // O(1) 查表
}
// 全卷
const itemScores = items.map(it => scoreItem(it.item_id, answer[it.item_id], score));
const total = sum(itemScores);
const mean  = total / items.length;
const RD    = itemScores.map(s => s - mean);   // 每题相对个人均值偏离
```
> 常模百分位另配 `norm_table`(总分→百分位),或由样本动态计算。

---

## 3. `3_indicator_map.json` — 指标映射(筛选 + 报告)

```json
{
  "version": "2026-07-02-v1",
  "map": [
    {
      "item_id": "Q1",
      "primary":   { "secondary": "C", "tertiary": "C2",
                     "observation_points": ["介入时机的把握", "介入方式的适宜有效性"] },
      "secondary_ind": { "secondary": "A", "tertiary": "A1",
                         "observation_points": ["对游戏本质特征的理解"] },
      "interview_focus": "自主生成游戏与安全规则之间的平衡"
    }
  ]
}
```
- 指标编码/名称必须与 `CLAUDE.md` 第3节完全一致(A/A1…C/C2)。
- 用途:筛题时保证 3 题主二级指标分散;报告时按指标归类。

---

## 4. `4_selection_rules.json` — R/P/G 筛选规则

```json
{
  "version": "2026-07-02-v1",
  "process_thresholds": {
    "duration_enhance_pct": 75,   "duration_strong_pct": 90,
    "revise_enhance": 2,          "revise_strong": 4,
    "oscillation_strong": 2,      "fast_answer_pct": 25
  },
  "R": { "pick": 2, "basis": "RD绝对值降序(结果性偏离)" },
  "P": { "pick": 2, "basis": "P-IVI/过程异常降序" },
  "G": { "pick": 2, "basis": "结果-过程关系最值得解释" },
  "merge": {
    "final_count": 3,
    "dedup": true,
    "coverage_rule": "尽量不选3道主二级指标相同的题(8题主指标为C)",
    "note": "最终3题≠最低分题;能力覆盖修正仅在候选同质时启用"
  }
}
```
- 输出:`final_interview_items = [{item_id, select_source:R/P/G, FES证据强度, trigger_reason}]`。

---

## 5. `5_knowledge_base.json` — 知识库(AI 问答核心)⭐

把每题 13 张表结构化。这是 AI 访谈的专业背景 + 触发/追问/编码引擎。

```json
{
  "version": "2026-07-02-v1",
  "items": [
    {
      "item_id": "Q1",
      "kb_id": "GSYG_01",
      "core_orientation": "考察教师能否在自主生成的玩水游戏与规则/安全/场地适宜性之间作出专业判断……",
      "empirical_note": "A/C为高价值路径,D靠前为主要风险;A延迟观察+后续讨论,C参与+渐进转场,B规则协商,D功能回归。",

      "paths": [                                          // ← 04表 专业路径与实证校准
        { "option": "A", "type": "高分锚点", "suggest_pos": "优先",
          "reason": "尊重自主游戏,规则教育放到兴趣稍退后的反思讨论", "evidence": ["E1","E2","E3","E5"] },
        { "option": "C", "type": "高分锚点", "suggest_pos": "优先",
          "reason": "进入游戏保护兴趣,渐进转场到适宜空间", "evidence": ["E1","E3","E4","E7"] },
        { "option": "B", "type": "协调路径", "suggest_pos": "中高",
          "reason": "肯定发现+规则协商,但过快换地方偏成人安排", "evidence": ["E3","E6"] },
        { "option": "D", "type": "风险路径", "suggest_pos": "不宜靠前",
          "reason": "功能回归+注意转移,忽视玩水兴趣", "missing": ["E1","E2","E4","E7"] }
      ],

      "evidence_points": [                                // ← 06表 E1-E7
        { "code": "E2", "name": "儿童游戏意义识别",
          "high": "能说明行为背后的兴趣/需要/经验/角色", "low": "只看到乱/慢/不守规则",
          "triggers": ["T1","T3"] }
      ],

      "biases": [                                         // ← 07表 P1-P7
        { "code": "P1", "name": "器械功能单一/注意转移优先",
          "signal": "D排首位或明显靠前,A/C被压低",
          "trigger": "T1", "script": ["Q1","Q2-D","Q3"], "missing": ["E1","E2","E4","E7"] }
      ],

      "triggers": [                                       // ← 08表 T1-T7(触发引擎)
        { "code": "T1", "priority": "高",
          "result_cond": "D排首位;D明显靠前;或D不在末位且A/C被压低",
          "process_cond": "用时短且D首=稳定功能化;D与A/C反复调整=冲突",
          "target": "确认是否只看篮球架功能而忽视玩水兴趣",
          "scripts": ["Q1","Q2-D","Q3"] },
        { "code": "T6", "priority": "低/中",
          "result_cond": "A/C前两位且D末位",
          "process_cond": "过程复杂可访谈权衡;简单可作稳定优势确认",
          "target": "确认能否完整说明尊重兴趣→判断适宜性→渐进转场→长期规则",
          "scripts": ["Q1","Q2-A","Q3"] }
      ],

      "scripts": [                                        // ← 09表 Q1-Q7 + Q-stop(对话生成)
        { "code": "Q1", "stage": "回放理解", "applies_to": ["所有规则"],
          "question": "刚才这个情境中，您最先注意到的是什么？您觉得孩子的行为背后可能表达了什么兴趣、需要或游戏经验？",
          "goal": "看见儿童行为背后的游戏意义", "evidence": ["E2","E3"],
          "next": "回答空泛→Q2;能说明具体意义→Q3" },
        { "code": "Q4", "stage": "现场策略", "applies_to": ["策略表达不具体"],
          "question": "如果在真实班级里遇到类似情况，您会对孩子说哪一句话，或具体做哪一个动作来支持他们？",
          "goal": "理念转化为具体回应", "evidence": ["E5"], "next": "仍空泛→记录不足;具体→Q5" },
        { "code": "Q-stop", "stage": "停止规则", "applies_to": ["证据充分"],
          "question": "您已说明了对孩子行为的理解、介入依据和后续支持方式，我们进入下一个情境。",
          "goal": "避免机械追问", "next": "进入下一题" }
      ],

      "anchors": [                                        // ← 10表 评分锚点(访谈后编码)
        { "level": "高",   "criteria": "指标体系下完整解释+具体可持续策略", "required": ["E1","E2","E3","E4","E5","E6","E7"], "coding": "高水平" },
        { "level": "中高", "criteria": "理解主要指标,个别环节不足", "required": ["E1","E2","E4","E5"], "weak": ["E6","E7"], "coding": "中高" },
        { "level": "证据不足", "criteria": "回答过短/空泛,无法判断", "action": "再追问一次具体做法,仍不足则记证据不足" }
      ],

      "suggestions": [                                    // ← 11表 个性化支持建议
        { "type": "主三级补强:对游戏行为的分析与回应",
          "when": "该题偏低或访谈无法围绕C2解释判断",
          "goal": "形成稳定的指标化专业判断", "activities": "重写'我为什么这样判断'和'现场如何回应'",
          "follow": "能否用规范指标语言解释自己的做法" }
      ]
    }
  ]
}
```

### AI 问答自动运作(伪代码)
```js
// 1. 命中触发规则
const fired = kb[itemId].triggers.filter(t =>
     matchResult(ranking, t.result_cond) &&        // 结果性条件
     matchProcess(processTags, t.process_cond));    // 过程性增强(可选)
const trigger = topByPriority(fired);
// 2. 取脚本序列,逐轮提问(每轮一问,先接住再追缺失证据)
let scripts = trigger.scripts;                       // e.g. ["Q1","Q2-D","Q3"]
for (const code of scripts) {
  if (remainingSeconds < 60) break;                  // <1分钟不再生成新问
  const q = kb[itemId].scripts.find(s => s.code === code);
  ask(q.question);                                   // AI 提问
  const ans = await teacherAnswer();
  ledger.update(q.evidence, ans);                    // 更新证据账本 E1-E7
  if (ledger.enough()) { ask(Q_stop); break; }       // 证据充分收束
}
// 3. 访谈后:按 anchors 给该题编码(高/中高/中/偏低/证据不足)
```

---

## 6. Excel 作者层 → JSON(可选但推荐)

研究团队不直接写 JSON,而填固定表头的 Excel,导入脚本转换:

| Excel 模板 | 关键列 |
|---|---|
| 题库模板 | item_id, kb_id, title, stem, optionA-D |
| **赋分模板** | item_id, **order(如 C>A>B>D)**, score(0-4) —— 每题24行 |
| 映射模板 | item_id, 主二级, 主三级, 主观测点, 次二级, 次三级, 访谈焦点 |
| 知识库模板 | 沿用现有 13 表结构(01-13),脚本按表映射到 JSON 字段 |

> 现有 `DOC/10个题目的知识库/*.xlsx` 已是 13 表结构,可直接作为知识库模板;注意其 `x:` 命名空间 + 内联 `t="str"` 存储,解析需匹配 `<x:row>/<x:v>`。

---

## 7. 导入校验(不过则拒绝入库)

1. 六处 `item_id` 一致:题库/赋分/映射/知识库(及题号引用)。
2. 每题 `by_order` 恰好 24 条,key 为 A/B/C/D 的合法排列,不重不漏;value ∈ [0,4]。
3. 指标编码 ∈ {A,A1,A2,A3,B,B1,B2,C,C1,C2},名称与规范一致。
4. 知识库中 `triggers.scripts`/`biases.script` 引用的 Q 码、E 码在本题内存在(引用完整性)。
5. `anchors` 覆盖 5 档;每题至少 1 条 `suggestions`。
6. 全部文件 `version` 一致。
